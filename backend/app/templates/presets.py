TEMPLATES = {
    "meeting_minutes": {
        "id": "meeting_minutes",
        "name": "标准会议纪要",
        "description": "包含会议主题、参会人员、讨论内容、决定事项和行动项",
        "system_prompt": """你是一位专业的会议记录秘书，擅长从会议转录文字中提取关键信息并整理成结构化的会议纪要。

请遵循以下规则：
1. 从转录内容中客观提取信息，不要添加任何不存在的观点或事实。
2. 如果会议中出现了中英文混用，请保持专业术语的原貌。
3. 对于无法确定的信息（如人名、具体日期），标注"[待确认]"。
4. 用简洁清晰的语言进行总结整理。""",
        "output_format": """# 会议纪要

## 一、会议基本信息
- **主题**：{摘要}
- **日期**：{日期}
- **参会人员**：{参会人员}

## 二、讨论内容
{按议题分段列出各方主要观点和讨论过程}

## 三、决定事项
{列出会议达成的共识和决策}

## 四、行动计划
| 序号 | 行动项 | 负责人 | 截止日期 |
|------|--------|--------|----------|
| 1    | ...    | ...    | ...      |""",
    },
    "action_items": {
        "id": "action_items",
        "name": "行动计划",
        "description": "提取会议中的待办事项，含负责人和截止日期",
        "system_prompt": """你是一位高效的项目管理员，负责从会议转录文字中提取所有行动项 (Action Items)。

要求：
1. 逐条列出会议中明确提出的所有待办/行动项。
2. 如果多人被分配了同一任务的不同部分，请拆分为独立条目。
3. 标注每个行动项的优先级（高/中/低）。
4. 如果没有明确的截止日期，标注"[待确认]"。""",
        "output_format": """# 行动计划

| 优先级 | 行动项 | 负责人 | 截止日期 | 备注 |
|--------|--------|--------|----------|------|
| 高     | ...    | ...    | ...      | ...  |""",
    },
    "quick_summary": {
        "id": "quick_summary",
        "name": "简要纪要",
        "description": "200字以内的简短摘要，适合快速同步",
        "system_prompt": """请将以下会议转录内容精炼为一段不超过200字的中文摘要，包含：
- 会议目的
- 关键结论/决定
- 下一步核心行动

要求简洁有力，无需编号或分点。""",
        "output_format": """{一行摘要}""",
    },
}


def get_templates() -> list[dict]:
    return [
        {"id": t["id"], "name": t["name"], "description": t["description"]}
        for t in TEMPLATES.values()
    ]


def get_template(template_id: str) -> dict | None:
    return TEMPlATES.get(template_id)
